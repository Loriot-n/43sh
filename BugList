echo "ls -al | grep Makefile;" éxécute la commande au lieu de l'afficher
echo plante quand il doit afficher quelque chose contenant un ':' (':' est dans la liste des bad tokens)
exit n'affiche pas de message d'erreurs si l'argument est invalide
la commande "exit && ls" marche sous tcsh, Dieu seul sait pourquoi
Tous les builtins s'éxécutent 2 fois
les || et les && ne marchent plus
source /dev/random segfault
Ctrl+C affiche deux fois le "$>"
la commande "caca > ok" affiche caca dans ok
aucune commande ne marche si pas de variable $PATH
le Ctrl+D n'affiche plus exit
si on fait des espaces/tab puis Ctr+D, rien ne se passe
./42sh < /dev/random segfault
Le 42sh catch toujous les segfault
l'affichege du SIGSEGV ou FPE ne marche pas (et fait planter tcsh :D)
